import * as React from 'react';

interface TelephoneBookProps {}

const TelephoneBook: React.FunctionComponent<TelephoneBookProps> = (props) => {
  return <div className="telephone-book"></div>;
};

export default TelephoneBook;
